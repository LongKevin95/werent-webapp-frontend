VITE_API_URL=http://localhost:8080
# Use the same OAuth Web client ID as backend GOOGLE_CLIENT_ID.
# Add both http://localhost and http://localhost:5173 to Authorized JavaScript
# origins in Google Cloud. Changes can take several minutes to become active.
VITE_GOOGLE_CLIENT_ID=<your-google-oauth-web-client-id>
